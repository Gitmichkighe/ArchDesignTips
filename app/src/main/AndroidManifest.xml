<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appdir">

    <!-- Required permissions for AdMob -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArchitecturalTips">


    <!-- Test AdMob App ID for development -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <!-- DisclaimerActivity is now the launcher activity -->
        <activity
            android:name=".DisclaimerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivityOld is no longer the launcher activity -->
        <activity android:name=".MainActivityOld" />
        <!-- Declare the RulesActivity -->
        <activity android:name=".RulesActivity"/>
        <!-- Other activities as necessary -->
        <activity android:name=".FavoritesActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".BookmarksActivity" />


        <activity android:name=".RewardedAdActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>


    </application>
</manifest>

